version: 3
automerge: false
parallel_plan: true
parallel_apply: true

projects:
  - name: aws-iac
    dir: .
    autoplan:
      enabled: true
      when_modified:
        - "*.tf"
        - "*.tfvars"
        - "**/*.tf"
        - "**/*.tfvars"
        - "**/*.hcl"


